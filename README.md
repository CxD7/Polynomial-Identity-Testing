# Polynomial-Identity-Testing
A Java program that verifies the identity of two polynomials using random sampling. It evaluates both polynomials at random points to detect data integrity or corruption with high probability.
